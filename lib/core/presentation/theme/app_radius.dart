/// Raios de borda padronizados do app.
class AppRadius {
  const AppRadius._();

  static const double sm = 8;
  static const double md = 12;
  static const double lg = 16;

  /// Cartões de destaque (herói do Resumo, cards de jornada) — o único
  /// raio acima de 16, reservado pro que precisa se destacar da lista.
  static const double xl = 20;
}
