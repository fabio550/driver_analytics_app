import 'package:flutter/material.dart';

/// Placeholder — o formulário de verdade vem no próximo passo.
class PromotionEarningCreatePage extends StatelessWidget {
  const PromotionEarningCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nova promoção')),
      body: const Center(child: Text('Formulário em construção.')),
    );
  }
}