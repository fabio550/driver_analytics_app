import 'package:flutter/material.dart';

/// Placeholder — o formulário de verdade vem no próximo passo.
class RideEarningCreatePage extends StatelessWidget {
  const RideEarningCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nova corrida')),
      body: const Center(child: Text('Formulário em construção.')),
    );
  }
}