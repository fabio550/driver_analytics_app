import 'package:flutter/material.dart';

/// Placeholder — o formulário de verdade vem no próximo passo.
class AdjustmentEarningCreatePage extends StatelessWidget {
  const AdjustmentEarningCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Novo ajuste')),
      body: const Center(child: Text('Formulário em construção.')),
    );
  }
}
