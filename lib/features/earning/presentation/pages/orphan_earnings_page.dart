import 'package:flutter/material.dart';

/// Placeholder — a atribuição manual a um turno vem no próximo passo.
class OrphanEarningsPage extends StatelessWidget {
  const OrphanEarningsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sem jornada')),
      body: const Center(child: Text('Em construção.')),
    );
  }
}