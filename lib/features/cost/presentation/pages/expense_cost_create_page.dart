import 'package:flutter/material.dart';

/// Placeholder — o formulário de verdade vem no próximo passo.
class ExpenseCostCreatePage extends StatelessWidget {
  const ExpenseCostCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nova despesa')),
      body: const Center(child: Text('Formulário em construção.')),
    );
  }
}
