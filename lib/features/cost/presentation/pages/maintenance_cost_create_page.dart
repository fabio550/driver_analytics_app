import 'package:flutter/material.dart';

/// Placeholder — o formulário de verdade vem no próximo passo.
class MaintenanceCostCreatePage extends StatelessWidget {
  const MaintenanceCostCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nova manutenção')),
      body: const Center(child: Text('Formulário em construção.')),
    );
  }
}
