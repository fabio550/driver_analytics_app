import 'package:flutter/material.dart';

/// Placeholder — o formulário de verdade vem no próximo passo.
class FuelCostCreatePage extends StatelessWidget {
  const FuelCostCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Novo abastecimento')),
      body: const Center(child: Text('Formulário em construção.')),
    );
  }
}
